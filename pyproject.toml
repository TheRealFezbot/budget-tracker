[project]
name = "budget-tracker"
version = "0.1.0"
description = "A personal budget tracking application built with FastAPI and SQLite."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.128.0",
    "sqlmodel>=0.0.31",
    "uvicorn>=0.40.0",
]
